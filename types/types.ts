import { RefObject } from "react";

export type WorkInfoProps = {
  title: string;
  desc: string;
  techn: string[];
};
export type WorkImgProps = {
  img: string;
  img2: string;
  img3: string;
  title: string;
};
export type FadeInProps = {
  children: React.ReactNode;
};
